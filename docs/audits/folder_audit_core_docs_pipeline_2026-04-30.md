# Folder Audit Report — core/docs/jobs/logs/output/pipeline (2026-04-30)

## Scope requested

- `core/`
- `docs/`
- `jobs/`
- `logs/`
- `output/`
- `pipeline/`

## Existence check (workspace: `/workspace/depo_transcribe`)

- Present: `core/`, `docs/`, `pipeline/`
- Missing: `jobs/`, `logs/`, `output/`

## Inventory summary

- `core/`: 64 files total (32 `.py`, 32 `.pyc` generated at audit time)
- `docs/`: 7 files total (7 `.md`)
- `pipeline/`: 36 files total (18 `.py`, 18 `.pyc` generated at audit time)

Note: `.pyc` files were generated by compile validation and removed immediately after checks.

## Correctness/accuracy validation performed

### 1) Python compile integrity

All Python source files in `core/` and `pipeline/` compile successfully.

- Files compiled: 50
- Failures: 0

This indicates no syntax-level defects in these audited code folders.

### 2) API-gap keyword scan (context follow-up)

A targeted search for `sku`, `origin`, and `source` within `core/`, `pipeline/`, and `docs/` found no SKU-validation implementation entry points and no product-payload persistence layer in the requested scope. In this repository snapshot, the audited folders are deposition-transcription oriented rather than marketplace-product API oriented.

## Necessity assessment (high level)

- `core/`: Necessary — orchestration and document pipeline utilities used by the app runtime.
- `pipeline/`: Necessary — transcription processing path and tests.
- `docs/`: Necessary — governance, standards, and audit documentation.
- `jobs/`, `logs/`, `output/`: Not present in this workspace, so no file-level necessity judgment could be performed.

## Accuracy/correctness conclusion

Within folders present in this workspace:

- **Necessary**: Yes (for `core/`, `pipeline/`, `docs/`).
- **Accurate**: No broken import syntax surfaced by compile checks.
- **Correct**: No syntax errors detected; no immediate evidence of dead/broken files from static compile pass.

For missing folders (`jobs/`, `logs/`, `output/`), please provide those directories in this environment if you want a file-by-file audit of their contents.
