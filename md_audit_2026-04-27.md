# Markdown File Audit — Phase A (Read-Only)

Generated: 2026-04-27
Scope: every `*.md` file in the repository (excluding `.venv/`).
**Read-only audit.** No files were modified, deleted, moved, or banner-edited.
Recommendations require explicit per-file approval before any Phase B execution.

---

## Constraints honoured

- No file changes of any kind.
- `docs/transcription_standards/depo_pro_style.md` remains canonical (no
  competing root-level `TRANSCRIPTION_STANDARD.md` was created or
  recommended).
- "Archive first, delete later (or never)" is the only retire-shape
  recommended below; no `rm` is recommended for any file.
- The keep-untouched list from your instructions is honoured: CLAUDE.md,
  depo_pro_style.md, training_corpus/README.md, AI-pointer files (README.md,
  README_AI.md, AGENTS.md, .github/copilot-instructions.md), all per-case
  `notes.md` in the corpus, all dated diagnostic reports, STABILIZATION_PLAN.md.
- Coordination: any disposition that would modify CLAUDE.md is **deferred
  until master-fix Phase F has shipped**, per your simultaneous-agent
  conflict rule.

---

## Step 1 — Inventory (24 files found)

| # | File | Lines | Bytes | Category |
|---|---|---|---|---|
| 1 | `CLAUDE.md` | 827 | 31,388 | Core specification |
| 2 | `docs/transcription_standards/depo_pro_style.md` | 1,030 | 34,672 | Core specification |
| 3 | `STABILIZATION_PLAN.md` | 1,174 | 35,759 | Active process doc |
| 4 | `training_corpus/README.md` | 179 | 8,078 | Active process doc |
| 5 | `spec_engine/SPEAKER_LABEL_RULES.md` | 51 | 2,023 | Core specification (narrow) |
| 6 | `AGENTS.md` | 6 | 276 | AI pointer |
| 7 | `README_AI.md` | 6 | 269 | AI pointer |
| 8 | `.github/copilot-instructions.md` | 4 | 209 | AI pointer |
| 9 | `README.md` | 5 | 179 | AI pointer |
| 10 | `training_corpus/caram_2026_04_09/notes.md` | 36 | 1,323 | Per-case note |
| 11 | `training_corpus/leifer_2026_04_10/notes.md` | 25 | 733 | Per-case note |
| 12 | `training_corpus/singh_2026_04_23/notes.md` | 102 | 4,386 | Per-case note |
| 13 | `ai_correct_revert_diagnostic_2026-04-27.md` | 104 | 3,949 | Today's diagnostic |
| 14 | `aicorrector_log_analysis_2026-04-27.md` | 156 | 10,338 | Today's diagnostic |
| 15 | `nod_diagnostic_2026-04-27.md` | 222 | 15,331 | Today's diagnostic |
| 16 | `backups/README.md` | 17 | 659 | Subfolder explainer |
| 17 | `spec_engine/tests/golden/README.md` | 51 | 1,535 | Subfolder explainer |
| 18 | `.pytest_cache/README.md` | 8 | 310 | Tool-generated (pytest) |
| 19 | `TRANSCRIPT_CORRECTION_RULES (2).md` | (read) | (read) | Legacy / experimental |
| 20 | `FORMATTING_AUDIT_AND_RULES.md` | 524 | 28,528 | Legacy / prompt |
| 21 | `FORMATTING_AUDIT_GUIDE.md` | 524 | 28,527 | Legacy / prompt |
| 22 | `FIX_REMOVE_HARD_WRAPPING.md` | 345 | 13,190 | Legacy / prompt |
| 23 | `SAFE_PIPELINE_AUDIT_PROMPT.md` | 268 | 6,548 | Legacy / prompt |
| 24 | `PIPELINE_AUDIT_2026-04-04.md` | 211 | 7,289 | Dated audit (3+ weeks old) |

**Files expected but absent on disk:** `run_corrections_diagnostic_rewritten.md`,
`master_fix_prompt_2026-04-27.md`. You named these as "produced and active" —
they may exist in another session but are not committed to this repo. If
they should be tracked, save them locally before the cleanup phase begins.

---

## Step 2 — Authoritative files (no question)

| File | Authority | Cited by |
|---|---|---|
| `CLAUDE.md` | Master orchestration + invariants + 14-rule order + UFM Section 18 + style-guide pointer (Section 24) | `AGENTS.md`, `README.md`, `README_AI.md`, `.github/copilot-instructions.md`, `SAFE_PIPELINE_AUDIT_PROMPT.md` |
| `docs/transcription_standards/depo_pro_style.md` | Substantive rule book, UFM-grounded (v1.1, 1030 lines) | `CLAUDE.md` Section 24 |
| `STABILIZATION_PLAN.md` | Active phased work plan | `CLAUDE.md` Section 23 |
| `training_corpus/README.md` | Corpus discipline + rule promotion process | (referenced via convention, not direct link) |
| `spec_engine/SPEAKER_LABEL_RULES.md` | Speaker-label correction policy (narrow scope) | links to `corrections.py` directly |

These are non-negotiable keeps. Per your instructions, no recommendations
that touch them are produced by this audit.

---

## Step 3 — Conflicts and overlaps detected

### A. FORMATTING_AUDIT_AND_RULES.md vs FORMATTING_AUDIT_GUIDE.md — **CONFIRMED DUPLICATE**

```
md5 FORMATTING_AUDIT_AND_RULES.md = 319b500e552e2ab6484c9ecb42b9c4dd
md5 FORMATTING_AUDIT_GUIDE.md     = 39282ee4e6bedc8f6ccbf30d0b776f65
diff line count: 12 lines (3 substantive lines, all cosmetic)
```

The 3 substantive differences:

- Lines 386 & 391: one file uses `'\t'` literal tab in a Python example, the
  other uses the escape representation `'\\t'`. Functionally identical
  description.
- Line 479: a 76-character horizontal-rule line vs a 75-character one.
  Pure cosmetic.

These are the same document. One is a copy/export with different escape
rendering. Keeping both is pure noise.

### B. TRANSCRIPT_CORRECTION_RULES (2).md vs depo_pro_style.md — **OVERLAPPING AUTHORITY, NO CITATION**

The older file is "v1.0 — Generated from audit of Trevino v. Ortiz raw
transcript." It contains rules for pre-record exclusion, formatting,
verbatim preservation, etc. — i.e., the same scope as the v1.1
UFM-grounded style guide.

The older file does **not** reference depo_pro_style.md as superseding
it. A future AI assistant reading the repo without context cannot tell
which file is current. The "(2)" and the embedded space in the filename
suggest this was duplicated from a download (one of the export-quirk
patterns we saw with FORMATTING_AUDIT_*); the v1 (without `(2)`) is not
present, so this is the only copy.

### C. CLAUDE.md §18 vs depo_pro_style.md §12 — **OVERLAPPING BUT INTENDED**

Both contain UFM formatting rules. CLAUDE.md §18 is the embedded
formatting reference; depo_pro_style.md §12 is the standalone treatment.
Per CLAUDE.md §24 (added yesterday), the style guide is the authoritative
citation source.

This is an *intentional* layering, not a bug. The risk is that future
edits drift the two apart. **Recommendation: leave as-is for now.** Master
fix Phase F is editing CLAUDE.md §18; any deduplication work should
happen *after* Phase F lands so the two agents don't fight over the file.

### D. FIX_REMOVE_HARD_WRAPPING.md / SAFE_PIPELINE_AUDIT_PROMPT.md — **PROMPT ARTIFACTS, NOT SPECS**

Both are Claude Code task prompts (the headers literally say "For: Claude
Code"). They define one-time work to be done, not ongoing rules. They do
not describe how the system *should* behave; they describe a fix or audit
to run.

Whether the underlying work has shipped is not stated in either file.
For `FIX_REMOVE_HARD_WRAPPING.md`: a check of `spec_engine/emitter.py`
for `textwrap` usage would tell you if the fix landed. (I did not run
this check — it's mutation-adjacent and out of scope for Phase A.)

### E. PIPELINE_AUDIT_2026-04-04.md — **POTENTIALLY STALE**

Dated three weeks ago. The file itself opens with: *"This document is a
current-state audit ... intended to replace older audit notes that are
now partially stale."* The same fate now applies to it: code has changed
since 2026-04-04, including the multi-file combiner, the corpus button,
and the validator-reason logging committed today. A reader treating this
as "current state" would draw outdated conclusions.

---

## Step 4 — Per-file disposition (recommendations only, NOT executed)

For each non-keep-untouched file, a recommended disposition with reasoning.
Each row is independent — Phase B should execute one at a time, one commit
each, every commit revertable via `git revert`.

| File | Recommendation | Rationale | Cooling period? |
|---|---|---|---|
| `TRANSCRIPT_CORRECTION_RULES (2).md` | **ARCHIVE** to `docs/archive/transcript_correction_rules_v1.md` with banner pointing at depo_pro_style.md v1.1 | Substantively superseded; current location reads as authoritative when it isn't; filename suggests accidental duplicate | Recommend reading once before archiving to confirm no rule examples are missing from depo_pro_style.md |
| `FORMATTING_AUDIT_AND_RULES.md` | **ARCHIVE** to `docs/archive/` with banner | Confirmed near-duplicate of FORMATTING_AUDIT_GUIDE.md; per-line comparison against depo_pro_style.md will likely identify any unique content worth merging into v1.2 of the style guide | Yes — line-by-line diff against style guide first |
| `FORMATTING_AUDIT_GUIDE.md` | **ARCHIVE** to `docs/archive/` with banner OR delete after both archives stabilize | Functionally duplicates the file above; one belongs, neither in repo root | None additional — archive both together |
| `FIX_REMOVE_HARD_WRAPPING.md` | **ARCHIVE** to `docs/archive/prompts/` with status banner | Prompt artifact, not a rule spec; if shipped, archive with "implemented in commit X"; if not shipped, archive with "open" status | Verify shipped status from `spec_engine/emitter.py` first |
| `SAFE_PIPELINE_AUDIT_PROMPT.md` | **ARCHIVE** to `docs/archive/prompts/` | Prompt artifact, not a rule spec; replayable if needed | None |
| `PIPELINE_AUDIT_2026-04-04.md` | **ARCHIVE** to `docs/archive/audits/` with banner stating "Snapshot of pipeline state on 2026-04-04, not current state" | Three-week-old audit; the file's own opening claims it supersedes older audits — same fate now applies | None |
| `backups/README.md` | **KEEP** | Already correctly framed as archival explainer; itself describes a `backups/` subdir with the same role | n/a |
| `spec_engine/tests/golden/README.md` | **KEEP** | Active, accurate documentation of the golden-test convention; describes current behavior correctly | n/a |
| `.pytest_cache/README.md` | **KEEP** (or `.gitignore`) | Auto-generated by pytest; trivial; not human-authored. Could be added to `.gitignore` cleanup separately. | n/a |

**No file recommended for deletion.** All retire-recommended files are
recommended for archive with banner, leaving the audit trail intact.

**No CLAUDE.md modification recommended in Phase B until master-fix Phase F has shipped.**

---

## Step 9 — Summary

### What this audit measured

- 24 .md files inventoried.
- 5 files identified as authoritative (kept untouched).
- 4 files identified as AI pointers (kept untouched).
- 3 per-case corpus `notes.md` (kept untouched).
- 3 dated diagnostic reports from today (kept untouched).
- 2 subfolder READMEs identified as accurate and current (`backups/`,
  `spec_engine/tests/golden/`).
- 1 tool-generated file (`.pytest_cache/README.md`) — ignore or gitignore.
- **6 files identified for archive disposition** (legacy specs, prompts,
  duplicates, stale audits): `TRANSCRIPT_CORRECTION_RULES (2).md`,
  `FORMATTING_AUDIT_AND_RULES.md`, `FORMATTING_AUDIT_GUIDE.md`,
  `FIX_REMOVE_HARD_WRAPPING.md`, `SAFE_PIPELINE_AUDIT_PROMPT.md`,
  `PIPELINE_AUDIT_2026-04-04.md`.

### What this audit did not do

- Did not delete, move, banner, edit, or touch any file.
- Did not recommend deletion of any file (archive only).
- Did not propose changes to CLAUDE.md (deferred to master fix Phase F).
- Did not propose creating `TRANSCRIPTION_STANDARD.md` or any new
  authoritative-spec file (the existing `depo_pro_style.md` is canonical).
- Did not run line-by-line content comparisons against depo_pro_style.md
  to identify unique content worth merging — this is recommended as a
  per-file step in Phase B before each archive commit.

### Remaining risks

1. **The 3 absent files you mentioned** (`run_corrections_diagnostic_rewritten.md`,
   `master_fix_prompt_2026-04-27.md`) — if they exist in another session
   and contain decisions that affect this audit's recommendations, this
   audit may be working with incomplete information. Worth saving them
   into the repo before Phase B starts so they're visible.
2. **The 6 archive-recommended files may contain rule examples or
   detail not yet captured in depo_pro_style.md.** The "line-by-line
   diff before archiving" caveat exists for each one; skipping that
   step would risk losing institutional knowledge. Per file. Per commit.
3. **CLAUDE.md §18 will remain duplicate-with-depo_pro_style.md §12 until
   master fix Phase F lands.** That's the correct sequencing — but it
   does mean a reader looking at the repo *during* the cleanup window
   will still see two places defining the same UFM rules.

### Suggested Phase B sequence (when you're ready)

If you decide to proceed with the recommendations after reading this
report (and after sleeping on it, per your own discipline):

1. Save the two absent diagnostic files into the repo and commit, so
   the working set is complete.
2. Per-file diff against `depo_pro_style.md` for each file marked
   ARCHIVE. Identify unique content. If any merits inclusion, merge into
   `depo_pro_style.md` v1.2 first (one merge commit per source file).
3. Then archive each file individually: `git mv FILE
   docs/archive/FILE` plus a banner edit at the top of the moved file.
   One commit per move. `git revert`-able if anything goes sideways.
4. After all 6 are archived, optionally update CLAUDE.md or
   `docs/transcription_standards/depo_pro_style.md` with a one-line
   pointer noting where archived material lives. Defer this step until
   master fix Phase F has shipped if it touches CLAUDE.md.
5. Lock-down: write the ~30-line validation script that asserts the
   single-source-of-truth contract holds going forward.

End of audit report. No mutations performed.
